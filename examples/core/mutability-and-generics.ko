// Decision state: AWAITING DECISION; illustrative baseline, not approved syntax.
import { print } from "koda:io"

fn identity<T>(value: T) -> T {
    value
}

export fn main() -> Unit {
    let label = identity("ready")
    mut count = 0
    count = count + 1
    print(label)
    if count == 1 {
        print("count updated")
    } else {
        print("unexpected count")
    }
}
